import React from 'react';
import {View, Button} from 'react-native';

const Home = () => {
  return (
    <View
      style={{
        alignItems: 'center',
        justifyContent: 'center',
        height: 600,
      }}></View>
  );
};

export default Home;
